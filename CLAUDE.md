# Portfolio Nendi Candra — AI Instructions

See `docs/03-AGENTS.md` for full instructions.

## TL;DR
- Astro SSG, pure CSS, Three.js + GSAP
- Dark brutalist aesthetic
- Don't add new libs without asking
- All CSS in `src/styles/global.css`
- Build: `npm run build && vercel --prod`
